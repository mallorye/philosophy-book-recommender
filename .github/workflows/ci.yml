name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install "ruff==0.9.4" pytest pandas
      - name: Lint
        run: ruff check . --exclude notebooks
      - name: Tests
        run: pytest tests/ -v